package com.spongecoach.domain;

/** A Player's answer for an Event they're on the attendance list for (see {@link EventAttendance}). */
public enum AttendanceStatus {
    PENDING,
    ATTENDING,
    DECLINED
}
